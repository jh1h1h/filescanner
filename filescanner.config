[Credentials in Files]
Linux Command: grep -r -i -E 'KEYWORDS' "$SEARCH_ROOT" EXTENSIONS
Linux Example: grep -r -i -E 'password|passwd|pwd|user|username' "$SEARCH_ROOT"  --include="*.txt" --include="*.conf" --include="*.config" --include="*.xml" --include="*.ini" --include="*.log"
Windows Command: Select-String -Path "$SEARCH_ROOT" -Pattern 'KEYWORDS' -Recurse EXTENSIONS
Windows Example: Select-String -Path "$SEARCH_ROOT" -Pattern 'password|passwd|pwd|user|username' -Recurse -Include *.txt,*.conf,*.config,*.xml,*.ini,*.log
Keywords: password, passwd, pwd, user, username
Extensions: *.txt, *.conf, *.config, *.xml, *.ini, *.log

[API Keys and Secrets]
Linux Command: grep -r -i -E 'KEYWORDS' "$SEARCH_ROOT" EXTENSIONS
Linux Example: grep -r -i -E 'api_key|apikey|secret|token|auth|credential|private_key' "$SEARCH_ROOT"  --include="*.txt" --include="*.conf" --include="*.config" --include="*.env" --include="*.properties" --include="*.json" --include="*.yml"
Windows Command: Select-String -Path "$SEARCH_ROOT" -Pattern 'KEYWORDS' -Recurse EXTENSIONS
Windows Example: Select-String -Path "$SEARCH_ROOT" -Pattern 'api_key|apikey|secret|token|auth|credential|private_key' -Recurse -Include *.txt,*.conf,*.config,*.env,*.properties,*.json,*.yml
Keywords: api_key, apikey, secret, token, auth, credential, private_key
Extensions: *.txt, *.conf, *.config, *.env, *.properties, *.json, *.yml

[Config and Backup Files]
Linux Command: find "$SEARCH_ROOT" -type f EXTENSIONS
Linux Example: find "$SEARCH_ROOT" -type f \( -name "*.conf" -o -name "*.config" -o -name "*.bak" -o -name "*.old" -o -name "*.backup" -o -name "*.env" -o -name "*.swp" \)
Windows Command: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File EXTENSIONS
Windows Example: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File -Include *.conf,*.config,*.bak,*.old,*.backup,*.env,*.swp
Extensions: *.conf, *.config, *.bak, *.old, *.backup, *.env, *.swp

[Files with Sensitive Names]
Linux Command: find "$SEARCH_ROOT" -type f EXTENSIONS
Linux Example: find "$SEARCH_ROOT" -type f \( -name "*password*" -o -name "*secret*" -o -name "*credential*" -o -name "*admin*" \)
Windows Command: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File EXTENSIONS
Windows Example: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File -Include *password*,*secret*,*credential*,*admin*
Extensions: *password*, *secret*, *credential*, *admin*

[History Files]
Linux Command: find "$SEARCH_ROOT" -type f FILES -exec cat {} \;
Linux Example: find "$SEARCH_ROOT" -type f \( -name ".*history" -o -name ".bash_history" -o -name ".zsh_history" -o -name ".mysql_history" \) -exec cat {} \;
Windows Command: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File FILES | Get-Content
Windows Example: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File -Include .*history,.bash_history,.zsh_history,.mysql_history | Get-Content
Files: .*history, .bash_history, .zsh_history, .mysql_history

[Database Files]
Linux Command: find "$SEARCH_ROOT" -type f EXTENSIONS
Linux Example: find "$SEARCH_ROOT" -type f \( -name "*.db" -o -name "*.sqlite" -o -name "*.sqlite3" -o -name "*.sql" \)
Windows Command: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File EXTENSIONS
Windows Example: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File -Include *.db,*.sqlite,*.sqlite3,*.sql
Extensions: *.db, *.sqlite, *.sqlite3, *.sql

[SSH and Private Keys]
Linux Command: find "$SEARCH_ROOT" -type f FILES
Linux Example: find "$SEARCH_ROOT" -type f \( -name "id_rsa" -o -name "id_dsa" -o -name "id_ecdsa" -o -name "*.pem" -o -name "*.key" -o -name "authorized_keys" \)
Windows Command: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File FILES
Windows Example: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File -Include id_rsa,id_dsa,id_ecdsa,*.pem,*.key,authorized_keys
Files: id_rsa, id_dsa, id_ecdsa, *.pem, *.key, authorized_keys

[Code Comments]
Linux Command: grep -r -i -E 'KEYWORDS' "$SEARCH_ROOT" EXTENSIONS
Linux Example: grep -r -i -E 'TODO|FIXME|XXX|HACK|bug|vulnerable|vuln|temp|temporary|test|debug' "$SEARCH_ROOT"  --include="*.php" --include="*.py" --include="*.js" --include="*.sh" --include="*.c" --include="*.cpp"
Windows Command: Select-String -Path "$SEARCH_ROOT" -Pattern 'KEYWORDS' -Recurse EXTENSIONS
Windows Example: Select-String -Path "$SEARCH_ROOT" -Pattern 'TODO|FIXME|XXX|HACK|bug|vulnerable|vuln|temp|temporary|test|debug' -Recurse -Include *.php,*.py,*.js,*.sh,*.c,*.cpp
Keywords: TODO, FIXME, XXX, HACK, bug, vulnerable, vuln, temp, temporary, test, debug
Extensions: *.php, *.py, *.js, *.sh, *.c, *.cpp

[CTF Flags]
Linux Command: grep -r -i -E 'KEYWORDS' "$SEARCH_ROOT"
Linux Example: grep -r -i -E 'flag{|FLAG{|CTF{|htb{|thm{' "$SEARCH_ROOT"
Windows Command: Select-String -Path "$SEARCH_ROOT" -Pattern 'KEYWORDS' -Recurse
Windows Example: Select-String -Path "$SEARCH_ROOT" -Pattern 'flag{|FLAG{|CTF{|htb{|thm{' -Recurse
Keywords: flag{, FLAG{, CTF{, htb{, thm{

[Backup Archives]
Linux Command: find "$SEARCH_ROOT" -type f EXTENSIONS
Linux Example: find "$SEARCH_ROOT" -type f \( -name "*.zip" -o -name "*.tar" -o -name "*.tar.gz" -o -name "*.tgz" -o -name "*.rar" -o -name "*.7z" \)
Windows Command: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File EXTENSIONS
Windows Example: Get-ChildItem -Path "$SEARCH_ROOT" -Recurse -File -Include *.zip,*.tar,*.tar.gz,*.tgz,*.rar,*.7z
Extensions: *.zip, *.tar, *.tar.gz, *.tgz, *.rar, *.7z